1. SELECT `nazwa` FROM `miejsca` ORDER BY (nazwa) DESC;
2. SELECT `cena` FROM `miejsca` WHERE `nazwa` = "Malbork";
3. SELECT `nazwa`, `cena`, `link_obraz` FROM `miejsca` WHERE `link_obraz` LIKE "0%";
4. SELECT `nazwa`, ROUND(AVG(wycieczki.liczba_doroslych)) FROM `miejsca` INNER JOIN wycieczki ON miejsca.id_miejsca = wycieczki.id_miejsca GROUP BY (`nazwa`);
5. CREATE TABLE `wyprawy`.`klienci` (`id_klienta` INT NOT NULL AUTO_INCREMENT , `imie` VARCHAR(20) NOT NULL , `nazwisko` VARCHAR(40) NOT NULL , `data_ur` DATE NOT NULL , PRIMARY KEY (`id_klienta`));
