1. INSERT INTO `logowanie`(`id`, `nick`, `haslo`) VALUES (3,'Jeremi','Jer123');
2. SELECT FLOOR(AVG(`rokUr`)) as "Średni rok urodzenia" FROM `uczestnicy`;
3. SELECT `imie`, `nazwisko`, logowanie.nick, logowanie.haslo FROM `uczestnicy` INNER JOIN logowanie ON uczestnicy.id = logowanie.id WHERE imie LIKE 'J%';
4. CREATE USER 'uczestnik'@'localhost' IDENTIFIED BY 'Ucz123&';
5. GRANT SELECT, UPDATE ON uczestnicy TO 'uczestnik'@localhost;