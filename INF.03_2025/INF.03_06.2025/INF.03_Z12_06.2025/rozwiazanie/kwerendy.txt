1. SELECT `nazwa`, `cena`, `opis` FROM `abonamenty`;
2. SELECT ROUND(AVG(`cena`), 2) as "Srednio_abonament" FROM `abonamenty`;
3. SELECT nazwa, cecha FROM abonamenty INNER JOIN szczegolyabonamentu ON abonamenty.id = Abonamenty_id INNER JOIN cechy ON cechy.id = Cechy_id WHERE abonamenty.id = 1;
4. SELECT `nazwa`, `opis` FROM `abonamenty` WHERE `opis` LIKE "%zdrowie%" OR `opis` LIKE "%opieką%";